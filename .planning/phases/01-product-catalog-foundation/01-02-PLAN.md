---
phase: 01-product-catalog-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - app/layout.tsx
  - app/globals.css
  - tailwind.config.ts
  - app/components/Navigation.tsx
  - app/components/Footer.tsx
autonomous: false
requirements:
  - DSGN-01
  - DSGN-02
  - DSGN-03
  - TECH-03

must_haves:
  truths:
    - "Site uses clean sans-serif typography (Inter) with monochrome + accent color palette"
    - "Navigation header is shared across all pages with links to Shop, About, and FAQ"
    - "Footer displays Instagram and Twitter social links"
    - "All layout elements are mobile-responsive with a max-width container"
    - "Root layout wraps all pages with Navigation and Footer"
  artifacts:
    - path: "app/components/Navigation.tsx"
      provides: "Site-wide navigation header with links to /, /shop, /about, /faq"
      min_lines: 30
    - path: "app/components/Footer.tsx"
      provides: "Footer with Instagram and Twitter social links"
      min_lines: 20
    - path: "app/layout.tsx"
      provides: "Root layout with Navigation and Footer, Inter font, global metadata"
      contains: "Navigation"
    - path: "tailwind.config.ts"
      provides: "Accent color defined, Inter font configured"
      contains: "accent"
  key_links:
    - from: "app/layout.tsx"
      to: "app/components/Navigation.tsx"
      via: "import and render in layout body"
      pattern: "import.*Navigation"
    - from: "app/components/Footer.tsx"
      to: "app/lib/constants.ts"
      via: "SOCIAL_LINKS import"
      pattern: "SOCIAL_LINKS"
    - from: "tailwind.config.ts"
      to: "app/globals.css"
      via: "Tailwind utility classes used throughout"
      pattern: "accent"
---

<objective>
Build the site-wide design system: update layout with Inter font, define the monochrome + accent color palette in Tailwind, create the Navigation header and Footer components, and update the root layout to wrap all pages.

Purpose: All pages depend on this shared layout. Building it in Wave 2 (parallel to Plan 03) allows homepage and content page plans to proceed immediately after.

Output: Updated layout.tsx, updated globals.css, updated tailwind.config.ts, new Navigation.tsx and Footer.tsx
</objective>

<execution_context>
@/Users/august/.claude/get-shit-done/workflows/execute-plan.md
@/Users/august/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-product-catalog-foundation/01-CONTEXT.md
@.planning/phases/01-product-catalog-foundation/01-RESEARCH.md
@.planning/phases/01-product-catalog-foundation/01-01-SUMMARY.md
@app/layout.tsx
@app/globals.css
@tailwind.config.ts
@app/lib/constants.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Tailwind config with accent color and update globals.css for shop design system</name>
  <files>tailwind.config.ts
app/globals.css</files>
  <action>
Update `tailwind.config.ts` to add the accent color and preserve existing config. Use a warm black/off-white monochrome palette with a pink/coral accent (inspired by the existing `.pay-now-btn` pink `#ec4899` already in the codebase — this matches the indie/zine aesthetic):

```typescript
import type { Config } from 'tailwindcss';

const config: Config = {
  content: [
    './pages/**/*.{js,ts,jsx,tsx,mdx}',
    './components/**/*.{js,ts,jsx,tsx,mdx}',
    './app/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        accent: {
          DEFAULT: '#ec4899',  // pink-500 — matches existing brand touches
          hover: '#db2777',    // pink-600
          light: '#fce7f3',    // pink-100 for backgrounds
        },
      },
      fontFamily: {
        sans: ['Inter', 'system-ui', 'sans-serif'],
      },
      gridTemplateColumns: {
        '13': 'repeat(13, minmax(0, 1fr))',
      },
    },
    keyframes: {
      shimmer: {
        '100%': {
          transform: 'translateX(100%)',
        },
      },
    },
  },
  plugins: [require('@tailwindcss/forms')],
};
export default config;
```

Append shop-specific utility classes to `app/globals.css` AFTER the existing imports and styles (do NOT remove existing styles — existing comic pages still need them):

```css
/* ============================================
   Shop Design System
   ============================================ */

/* Inter font for shop pages */
@import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap');

/* Shop layout containers */
.shop-page {
  min-height: 100vh;
  display: flex;
  flex-direction: column;
  font-family: 'Inter', system-ui, sans-serif;
  background-color: #ffffff;
  color: #111111;
}

.shop-container {
  max-width: 1200px;
  margin: 0 auto;
  padding: 0 1rem;
  width: 100%;
}

@media (min-width: 640px) {
  .shop-container {
    padding: 0 1.5rem;
  }
}

@media (min-width: 1024px) {
  .shop-container {
    padding: 0 2rem;
  }
}

/* Product grid */
.products-grid {
  display: grid;
  grid-template-columns: 1fr;
  gap: 1.5rem;
}

@media (min-width: 640px) {
  .products-grid {
    grid-template-columns: repeat(2, 1fr);
  }
}

@media (min-width: 1024px) {
  .products-grid {
    grid-template-columns: repeat(3, 1fr);
    gap: 2rem;
  }
}

/* Product card */
.shop-product-card {
  border: 1px solid #e5e7eb;
  border-radius: 8px;
  overflow: hidden;
  background: #ffffff;
  transition: box-shadow 0.2s ease;
  text-decoration: none;
  color: inherit;
  display: block;
}

.shop-product-card:hover {
  box-shadow: 0 4px 16px rgba(0, 0, 0, 0.1);
}

.shop-product-card.sold-out {
  opacity: 0.55;
}

.shop-product-card__image {
  aspect-ratio: 1;
  overflow: hidden;
  background-color: #f9fafb;
}

.shop-product-card__body {
  padding: 0.875rem 1rem;
}

.shop-product-card__name {
  font-size: 0.9375rem;
  font-weight: 600;
  margin: 0 0 0.25rem;
  color: #111111;
  line-height: 1.3;
}

.shop-product-card__price {
  font-size: 0.9375rem;
  color: #374151;
  margin: 0;
}

.shop-product-card__badge {
  display: inline-block;
  font-size: 0.75rem;
  font-weight: 600;
  padding: 0.2rem 0.5rem;
  border-radius: 4px;
  margin-top: 0.5rem;
  background-color: #f3f4f6;
  color: #6b7280;
  letter-spacing: 0.02em;
  text-transform: uppercase;
}

.shop-product-card__badge--sold-out {
  background-color: #fef2f2;
  color: #ef4444;
}

.shop-product-card__badge--pre-order {
  background-color: #fffbeb;
  color: #d97706;
}

/* Featured products row (homepage) */
.featured-row {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 1.5rem;
}

@media (min-width: 768px) {
  .featured-row {
    grid-template-columns: repeat(4, 1fr);
  }
}
```
  </action>
  <verify>
    <automated>cd /Volumes/Workspace/weekend/alice && pnpm tsc --noEmit 2>&1 | head -20</automated>
    <manual>Confirm tailwind.config.ts has accent color defined; confirm globals.css has shop design system section appended</manual>
  </verify>
  <done>tailwind.config.ts has accent color (#ec4899) and Inter font family; globals.css has all shop CSS classes appended without removing existing styles; TypeScript compiles</done>
</task>

<task type="auto">
  <name>Task 2: Create Navigation and Footer components, update root layout</name>
  <files>app/components/Navigation.tsx
app/components/Footer.tsx
app/layout.tsx</files>
  <action>
Create `app/components/Navigation.tsx` — sticky top navigation with site name logo + links to Shop, About, FAQ. Uses Inter font (via shop-container CSS class or inline styles). Mobile-responsive: shows nav links on desktop, collapses gracefully on mobile (no hamburger needed for under-5-links at this scale):

```typescript
import Link from 'next/link'

export default function Navigation() {
  return (
    <header className="bg-white border-b border-gray-200 sticky top-0 z-50" style={{ fontFamily: "'Inter', system-ui, sans-serif" }}>
      <div className="shop-container">
        <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', height: '3.5rem' }}>
          <Link
            href="/"
            style={{ fontWeight: 700, fontSize: '1rem', letterSpacing: '-0.02em', color: '#111111', textDecoration: 'none' }}
          >
            centimentalcomics
          </Link>
          <nav>
            <ul style={{ display: 'flex', gap: '1.5rem', listStyle: 'none', margin: 0, padding: 0, alignItems: 'center' }}>
              <li>
                <Link href="/shop" style={{ fontSize: '0.9rem', color: '#374151', textDecoration: 'none', fontWeight: 500 }}
                  className="hover:text-accent">
                  shop
                </Link>
              </li>
              <li>
                <Link href="/about" style={{ fontSize: '0.9rem', color: '#374151', textDecoration: 'none', fontWeight: 500 }}
                  className="hover:text-accent">
                  about
                </Link>
              </li>
              <li>
                <Link href="/faq" style={{ fontSize: '0.9rem', color: '#374151', textDecoration: 'none', fontWeight: 500 }}
                  className="hover:text-accent">
                  faq
                </Link>
              </li>
            </ul>
          </nav>
        </div>
      </div>
    </header>
  )
}
```

Create `app/components/Footer.tsx` — minimal footer with social links and copyright. Import SOCIAL_LINKS from constants:

```typescript
import Link from 'next/link'
import { SOCIAL_LINKS } from '@/app/lib/constants'

export default function Footer() {
  return (
    <footer
      style={{
        borderTop: '1px solid #e5e7eb',
        paddingTop: '2rem',
        paddingBottom: '2rem',
        marginTop: 'auto',
        fontFamily: "'Inter', system-ui, sans-serif",
      }}
    >
      <div className="shop-container">
        <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', gap: '0.75rem' }}>
          <div style={{ display: 'flex', gap: '1.25rem' }}>
            <a
              href={SOCIAL_LINKS.instagram}
              target="_blank"
              rel="noopener noreferrer"
              style={{ fontSize: '0.875rem', color: '#6b7280', textDecoration: 'none' }}
              className="hover:text-accent"
            >
              instagram
            </a>
            <a
              href={SOCIAL_LINKS.twitter}
              target="_blank"
              rel="noopener noreferrer"
              style={{ fontSize: '0.875rem', color: '#6b7280', textDecoration: 'none' }}
              className="hover:text-accent"
            >
              twitter
            </a>
          </div>
          <p style={{ fontSize: '0.8rem', color: '#9ca3af', margin: 0 }}>
            © {new Date().getFullYear()} centimentalcomics · made in ph
          </p>
        </div>
      </div>
    </footer>
  )
}
```

Update `app/layout.tsx` to:
1. Switch from `Coming_Soon` Google Font to `Inter` (via next/font/google)
2. Import and render Navigation and Footer
3. Wrap children in a flex column so footer stays at bottom
4. Keep all existing metadata (do NOT remove it — it's well configured)

The updated layout should look like:

```typescript
import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"
import { GoogleTagManager, GoogleAnalytics } from "@next/third-parties/google"
import Navigation from "@/app/components/Navigation"
import Footer from "@/app/components/Footer"

const inter = Inter({
  subsets: ["latin"],
  display: "swap",
})

// Keep all existing metadata unchanged
export const metadata: Metadata = {
  title: {
    template: "centimentalcomics: %s",
    default: "centimentalcomics",
  },
  description: "some comics about art and internet",
  keywords: ["comics", "art", "internet", "webcomics", "illustration"],
  authors: [{ name: "ce manalang" }],
  creator: "ce manalang",
  publisher: "centimentalcomics",
  openGraph: {
    type: "website",
    locale: "en_US",
    url: "https://centimentalcomics.com",
    siteName: "centimentalcomics",
    title: "centimentalcomics",
    description: "some comics about art and internet",
    images: [
      {
        url: "/assets/images/og-image.jpg",
        width: 1200,
        height: 630,
        alt: "centimentalcomics",
      },
    ],
  },
  twitter: {
    card: "summary_large_image",
    title: "centimentalcomics",
    description: "some comics about art and internet",
    images: ["/assets/images/og-image.jpg"],
    creator: "@centimentalcomx",
  },
  robots: {
    index: true,
    follow: true,
    googleBot: {
      index: true,
      follow: true,
      "max-image-preview": "large",
      "max-snippet": -1,
    },
  },
  icons: {
    icon: "/favicon.ico",
    shortcut: "/favicon.ico",
    apple: "/apple-touch-icon.png",
  },
  manifest: "/site.webmanifest",
  metadataBase: new URL("https://centimentalcomics.com"),
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  const gtmId = process.env.NEXT_PUBLIC_GTM_ID
  const gaId = process.env.NEXT_PUBLIC_GA_MEASUREMENT_ID

  return (
    <html lang="en">
      {gtmId && <GoogleTagManager gtmId={gtmId} />}
      {gaId && <GoogleAnalytics gaId={gaId} />}
      <body className={inter.className} style={{ margin: 0, display: 'flex', flexDirection: 'column', minHeight: '100vh' }}>
        <Navigation />
        <main style={{ flex: 1 }}>
          {children}
        </main>
        <Footer />
      </body>
    </html>
  )
}
```

IMPORTANT: The existing `app/page.tsx` (comic homepage) has its own inline header/nav/footer — those will now be visually duplicated with the root layout. This is expected and acceptable for now. The shop pages use the root layout Navigation/Footer. The old comic page layout will be cleaned up if needed in a follow-up task within Plan 05 when the homepage is rebuilt.
  </action>
  <verify>
    <automated>cd /Volumes/Workspace/weekend/alice && pnpm tsc --noEmit 2>&1 | head -30</automated>
    <manual>Run `pnpm dev` and visit http://localhost:3000 — confirm Navigation header and Footer appear on all pages; confirm font changed to Inter</manual>
  </verify>
  <done>Navigation.tsx renders site links (shop, about, faq); Footer.tsx renders Instagram + Twitter links from SOCIAL_LINKS constant; layout.tsx uses Inter font and wraps all pages with Navigation + Footer; TypeScript compiles without errors</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify design system and navigation in browser</name>
  <action>Human verification of the design system — confirm Navigation header, Footer, Inter font, and accent color are rendering correctly across pages. Claude has already completed all automated work in Tasks 1 and 2.</action>
  <what-built>Design system foundation: Inter font, monochrome + accent color palette, sticky Navigation header with links to shop/about/faq, Footer with social links. Root layout wraps all pages.</what-built>
  <how-to-verify>
    1. Run `pnpm dev` in the project directory
    2. Visit http://localhost:3000 — confirm Navigation header appears at top (logo left, nav links right)
    3. Confirm footer appears at bottom with Instagram and Twitter links
    4. Confirm font has changed from "Coming Soon" to Inter (clean sans-serif)
    5. Visit http://localhost:3000/shop — confirm Navigation and Footer appear
    6. Resize browser to mobile width (< 640px) — confirm nav links remain accessible
  </how-to-verify>
  <resume-signal>Type "approved" if layout looks correct, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
- `pnpm tsc --noEmit` passes
- Navigation component exists at `app/components/Navigation.tsx` with links to /, /shop, /about, /faq
- Footer component exists at `app/components/Footer.tsx` with Instagram and Twitter links
- layout.tsx imports and renders both Navigation and Footer
- tailwind.config.ts has accent color defined
- globals.css has shop design system CSS appended (not replacing existing CSS)
</verification>

<success_criteria>
Design system is complete when:
- Visiting any page shows the Navigation header and Footer
- Navigation links point to correct URLs: / (home), /shop, /about, /faq
- Footer shows Instagram and Twitter social links (from SOCIAL_LINKS constant)
- Inter font is rendering instead of Coming Soon
- No TypeScript compilation errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-product-catalog-foundation/01-02-SUMMARY.md` documenting:
- Files created/modified
- Accent color chosen (#ec4899 pink-500) and rationale
- CSS class naming conventions used (shop-* prefix)
- Font choice (Inter via next/font/google)
- Note about existing comic page having its own nav (cleanup in Plan 05)
</output>
