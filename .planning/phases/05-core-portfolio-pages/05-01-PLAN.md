---
phase: 05-core-portfolio-pages
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/page.tsx
  - app/(portfolio)/legacy-comics/page.tsx
  - app/components/portfolio-navigation.tsx
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - HOME-01
  - HOME-02
  - HOME-03
  - HOME-04
  - ENG-01
  - ENG-02
  - RESM-01
  - RESM-02

must_haves:
  truths:
    - Homepage route ownership is explicitly resolved: portfolio homepage is authoritative at `/`
    - Existing comics root content is preserved at a non-root portfolio-safe path (for example `/legacy-comics`)
    - Portfolio navigation Home link continues to point to `/` and now lands on portfolio homepage content
    - No broken internal links are introduced by the route ownership change
    - Planning/state docs reflect the route ownership decision for downstream plans
  artifacts:
    - app/page.tsx
    - app/(portfolio)/legacy-comics/page.tsx
    - app/components/portfolio-navigation.tsx
    - .planning/ROADMAP.md
    - .planning/STATE.md
  key_links:
    - `app/page.tsx` becomes Phase 5 homepage implementation entrypoint
    - `app/components/portfolio-navigation.tsx` home IA and route ownership stay aligned
    - Route ownership decision recorded in planning docs for execution traceability
---

<objective>
Resolve homepage route ownership as a blocking architecture concern, then scaffold the portfolio homepage at `/` so all HOME requirements can be implemented and verified against the roadmap intent.

Output: `/` is explicitly portfolio-owned, legacy comics content is preserved at a secondary path, and homepage section scaffold exists for hero, strengths, featured case studies, and timeline.
</objective>

<execution_context>
@/Users/august/.claude/get-shit-done/workflows/execute-plan.md
@/Users/august/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-core-portfolio-pages/05-CONTEXT.md
@.planning/phases/05-core-portfolio-pages/05-RESEARCH.md

Blocking finding from research:
- `app/page.tsx` currently serves comics content while Phase 5 requires portfolio homepage behavior at `/`.

Locked for this phase:
- Portfolio homepage requirements (HOME-01..HOME-04) are satisfied at `/`, not on a secondary route.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make `/` the canonical portfolio homepage route</name>
  <files>
    app/page.tsx,
    app/(portfolio)/legacy-comics/page.tsx,
    app/components/portfolio-navigation.tsx
  </files>
  <action>
1. Move current comics homepage implementation out of `app/page.tsx` into `app/(portfolio)/legacy-comics/page.tsx` (or equivalent non-root path) so content is preserved.
2. Replace `app/page.tsx` with a portfolio homepage scaffold that includes semantic section anchors for:
   - hero
   - strengths
   - featured-case-studies
   - timeline
3. Keep Home navigation link in `app/components/portfolio-navigation.tsx` pointing to `/` and verify no conflicting route assumptions remain.
4. Ensure CTA placeholders in hero point to `/case-studies` and `/resume`.
  </action>
  <verify>
    `pnpm dev` boots; `/` renders portfolio homepage scaffold; `/legacy-comics` (or chosen path) renders prior comics homepage content; `/engineering`, `/resume`, `/case-studies`, `/contact` remain reachable.
  </verify>
  <done>Route ownership is resolved and homepage scaffolding is in place at `/`.</done>
</task>

<task type="auto">
  <name>Task 2: Record ownership decision in planning docs for downstream execution clarity</name>
  <files>
    .planning/ROADMAP.md,
    .planning/STATE.md
  </files>
  <action>
Add/update concise decision notes confirming homepage ownership at `/` so Plans 05-02 through 05-05 execute against the same route assumptions.
  </action>
  <verify>
    ROADMAP/STATE mention the explicit Phase 5 homepage routing decision and no contradiction remains with prior notes.
  </verify>
  <done>Route ownership is documented and downstream plans have clear implementation target.</done>
</task>

</tasks>

<verification>
1. `/` shows portfolio homepage scaffold and navigation.
2. Home link in portfolio nav routes to `/`.
3. Legacy comics content is available at the chosen non-root path.
4. No regressions on portfolio route-group pages.
5. Planning docs explicitly capture route ownership decision.
</verification>

<success_criteria>
- Route ownership blocker resolved before content work.
- `/` is portfolio homepage authority for all HOME requirements.
- Existing comics content remains accessible at a non-root path.
- Downstream plans can implement HOME requirements without ambiguity.
</success_criteria>

<output>
Create `.planning/phases/05-core-portfolio-pages/05-01-SUMMARY.md` with route ownership decision, files moved/edited, and any route-link impacts.
</output>
