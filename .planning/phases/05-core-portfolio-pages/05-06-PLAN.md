---
phase: 05-core-portfolio-pages
plan: "06"
type: execute
mode: gap_closure
gap_closure: true
wave: 4
depends_on:
  - "05-05"
files_modified:
  - app/page.tsx
  - app/(portfolio)/portfolio/page.tsx
  - app/components/portfolio-navigation.tsx
  - app/(portfolio)/layout.tsx
  - app/(portfolio)/engineering/page.tsx
  - app/(portfolio)/resume/page.tsx
  - app/lib/portfolio-data.ts
autonomous: true
requirements:
  - HOME-01
  - HOME-02
  - HOME-03
  - HOME-04
  - ENG-01
  - ENG-02
  - RESM-01
  - RESM-02

must_haves:
  truths:
    - Root route `/` is comics homepage authority and no longer serves portfolio home content
    - Portfolio homepage route is `/portfolio` and contains the Phase 5 HOME-01..HOME-04 experience
    - Portfolio primary navigation Home target points to `/portfolio` and stays consistent across all portfolio pages
    - All Phase 5 internal link contracts align with prefixed portfolio home ownership (no stale `/` assumptions)
    - Existing engineering and resume routes continue to work and remain reachable from portfolio navigation
  artifacts:
    - app/page.tsx
    - app/(portfolio)/portfolio/page.tsx
    - app/components/portfolio-navigation.tsx
    - app/(portfolio)/layout.tsx
    - app/(portfolio)/engineering/page.tsx
    - app/(portfolio)/resume/page.tsx
    - app/lib/portfolio-data.ts
  key_links:
    - `app/page.tsx` restores comics-first ownership at root
    - `app/(portfolio)/portfolio/page.tsx` becomes canonical HOME-01..HOME-04 implementation target
    - `app/components/portfolio-navigation.tsx` defines authoritative portfolio home link contract (`/portfolio`)
    - Portfolio page CTAs and cross-links remain valid under the new route contract
---

<objective>
Close the failed Phase 5 checkpoint by restoring root ownership to comics and relocating the portfolio homepage experience to `/portfolio`, while aligning all portfolio navigation and link contracts with that route model.

Output: Root route behavior and portfolio routing contracts match verification expectations without widening scope beyond Phase 5 gap remediation.
</objective>

<execution_context>
@/Users/august/.claude/get-shit-done/workflows/execute-plan.md
@/Users/august/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-core-portfolio-pages/05-VERIFICATION.md
@.planning/phases/05-core-portfolio-pages/05-CONTEXT.md
@.planning/phases/05-core-portfolio-pages/05-RESEARCH.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

Locked gap scope from failed checkpoint:
1. `/` must remain comics homepage.
2. Portfolio home must be `/portfolio`.
3. Navigation/link contracts must align to the above.

Out of scope:
- New content expansion beyond existing HOME/ENG/RESM Phase 5 deliverables.
- Phase 6 case study/contact feature work.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reassign route ownership to match verification contract</name>
  <files>
    app/page.tsx,
    app/(portfolio)/portfolio/page.tsx
  </files>
  <action>
1. Ensure `app/page.tsx` serves comics homepage behavior/content.
2. Move or duplicate current portfolio home implementation into `app/(portfolio)/portfolio/page.tsx` as the canonical Phase 5 home.
3. Confirm HOME-01..HOME-04 sections (hero, strengths, featured case studies, timeline) render at `/portfolio`.
  </action>
  <verify>
    `pnpm dev` shows comics homepage at `/` and full portfolio homepage at `/portfolio`.
  </verify>
  <done>Root and portfolio route ownership match required verification contract.</done>
</task>

<task type="auto">
  <name>Task 2: Align navigation and internal link contracts to `/portfolio`</name>
  <files>
    app/components/portfolio-navigation.tsx,
    app/(portfolio)/layout.tsx,
    app/(portfolio)/engineering/page.tsx,
    app/(portfolio)/resume/page.tsx,
    app/lib/portfolio-data.ts
  </files>
  <action>
1. Update portfolio Home nav destination to `/portfolio`.
2. Sweep portfolio page links/CTAs for stale root-home assumptions and normalize to the `/portfolio` contract where appropriate.
3. Keep `/engineering`, `/resume`, `/case-studies`, `/contact` behavior intact and reachable from navigation.
4. Do not alter content scope; only route/link contract adjustments needed for verification pass.
  </action>
  <verify>
    All primary portfolio nav items resolve correctly, with Home routing to `/portfolio` and no broken links from homepage CTAs/cards.
  </verify>
  <done>Portfolio navigation and link contracts are internally consistent with prefixed portfolio home routing.</done>
</task>

</tasks>

<verification>
1. `/` renders comics homepage (not portfolio home).
2. `/portfolio` renders full Phase 5 homepage experience (HOME-01..HOME-04).
3. Portfolio nav Home link points to `/portfolio`.
4. `/engineering` and `/resume` remain accessible and consistent.
5. Link check finds no stale `/`-as-portfolio-home assumptions.
</verification>

<success_criteria>
- Failed route-ownership checkpoint is remediated exactly as requested.
- Portfolio route contracts are coherent and testable for human rerun.
- No broad redesign or non-gap scope changes are introduced.
</success_criteria>

<output>
Create `.planning/phases/05-core-portfolio-pages/05-06-SUMMARY.md` with route changes, nav/link contract deltas, and verification notes for rerun readiness.
</output>
