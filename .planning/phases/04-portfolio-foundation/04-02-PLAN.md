---
phase: 04-portfolio-foundation
plan: "02"
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - app/(portfolio)/layout.tsx
  - app/(portfolio)/engineering/page.tsx
  - app/(portfolio)/case-studies/page.tsx
  - app/(portfolio)/resume/page.tsx
  - app/(portfolio)/contact/page.tsx
  - app/components/portfolio-navigation.tsx
  - app/components/portfolio-footer.tsx
  - app/globals.css
autonomous: true
requirements:
  - SITE-01
  - SITE-02
  - SITE-04
  - SITE-05

must_haves:
  truths:
    - Visiting /engineering shows portfolio navigation with links to Home, Engineering, Case Studies, Resume, Contact — no shop navigation visible
    - Visiting /case-studies shows portfolio navigation — no shop navigation visible
    - Visiting /resume shows portfolio navigation — no shop navigation visible
    - Visiting /contact shows portfolio navigation — no shop navigation visible
    - Portfolio footer contains a link to /shop (demoting shop to footer per SITE-02)
    - Portfolio pages use a professional neutral/dark palette (no pink #ec4899)
    - Each portfolio page has SEO title and description metadata in page source
    - Portfolio nav uses a different font from shop (NOT Inter)
  artifacts:
    - app/(portfolio)/layout.tsx
    - app/(portfolio)/engineering/page.tsx
    - app/(portfolio)/case-studies/page.tsx
    - app/(portfolio)/resume/page.tsx
    - app/(portfolio)/contact/page.tsx
    - app/components/portfolio-navigation.tsx
    - app/components/portfolio-footer.tsx
  key_links:
    - (portfolio)/layout.tsx imports portfolio-navigation and portfolio-footer
    - portfolio-footer contains shop link (SITE-02 compliance)
    - Each page.tsx exports metadata (SITE-04 compliance)
---

<objective>
Create the `(portfolio)` route group with its own layout (navigation, footer, CSS foundation, base metadata) and stub pages for all five portfolio routes: /engineering, /case-studies, /resume, /contact, plus a portfolio home placeholder.

Purpose: Establishes the visible portfolio section on the live site. Hiring managers can navigate all portfolio routes. Content is placeholder ("coming soon") — Phases 5-6 fill in real content.

Output: Four accessible portfolio pages at /engineering, /case-studies, /resume, /contact — each showing portfolio nav, portfolio footer, and correct SEO metadata.
</objective>

<execution_context>
@/Users/august/.claude/get-shit-done/workflows/execute-plan.md
@/Users/august/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-portfolio-foundation/04-01-SUMMARY.md
@.planning/ROADMAP.md

<interfaces>
<!-- Locked decisions from CONTEXT.md — NON-NEGOTIABLE -->

Navigation links (SITE-01):
  Home → / (comics root — portfolio "Home" links back to site root per CONTEXT.md decision)
  Engineering → /engineering
  Case Studies → /case-studies
  Resume → /resume
  Contact → /contact

Portfolio visual identity (LOCKED):
  - NO pink (#ec4899) anywhere in portfolio
  - Neutral/dark professional palette
  - System fonts or professional sans-serif — NOT Inter (Inter is shop)
  - CSS prefix: portfolio-* (parallel to shop-* convention in globals.css)

Shop in footer (SITE-02 — LOCKED):
  - Shop link appears in portfolio FOOTER only, not in portfolio nav

Discretion areas (Claude decides):
  - Exact color palette (neutral/dark professional)
  - Font choice (system fonts or professional option, NOT Inter)
  - Portfolio footer content beyond shop link
  - SEO metadata text per page

<!-- Existing CSS pattern from app/globals.css to extend -->
<!-- shop-* prefix convention: .shop-container, .shop-page, .shop-product-card etc. -->
<!-- New portfolio-* classes follow same pattern -->

<!-- From app/lib/constants.ts -->
export const SOCIAL_LINKS = {
  instagram: 'https://instagram.com/centimentalcomics',
  twitter: 'https://twitter.com/centimentalcomx',
}
export const SITE_URL = 'https://centimentalcomics.com'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create portfolio navigation, footer components, and CSS foundation</name>
  <files>
    app/components/portfolio-navigation.tsx,
    app/components/portfolio-footer.tsx,
    app/globals.css
  </files>
  <action>
**Step 1: Create app/components/portfolio-navigation.tsx**

Server component (no client directives needed — no hooks, no interactivity). Uses Next.js `Link` for navigation.

Font: Use system font stack for professional feel (not Inter). Apply via inline style or CSS class.

```typescript
import Link from "next/link"

export default function PortfolioNav() {
  return (
    <header className="portfolio-nav-header">
      <div className="portfolio-container">
        <nav className="portfolio-nav" aria-label="Portfolio navigation">
          <div className="portfolio-nav-brand">
            <Link href="/" className="portfolio-nav-brand-link">
              centimentalcomics
            </Link>
          </div>
          <ul className="portfolio-nav-list">
            <li>
              <Link href="/" className="portfolio-nav-link">home</Link>
            </li>
            <li>
              <Link href="/engineering" className="portfolio-nav-link">engineering</Link>
            </li>
            <li>
              <Link href="/case-studies" className="portfolio-nav-link">case studies</Link>
            </li>
            <li>
              <Link href="/resume" className="portfolio-nav-link">resume</Link>
            </li>
            <li>
              <Link href="/contact" className="portfolio-nav-link">contact</Link>
            </li>
          </ul>
        </nav>
      </div>
    </header>
  )
}
```

**Step 2: Create app/components/portfolio-footer.tsx**

Server component. Must include shop link (SITE-02: shop link is demoted to portfolio footer, not primary nav).

```typescript
import Link from "next/link"

export default function PortfolioFooter() {
  return (
    <footer className="portfolio-footer">
      <div className="portfolio-container">
        <div className="portfolio-footer-inner">
          <div className="portfolio-footer-links">
            <Link href="/shop" className="portfolio-footer-link">shop</Link>
            <a
              href="https://github.com/centimentalcomics"
              target="_blank"
              rel="noopener noreferrer"
              className="portfolio-footer-link"
            >
              github
            </a>
          </div>
          <p className="portfolio-footer-copy">
            &copy; {new Date().getFullYear()} ce manalang
          </p>
        </div>
      </div>
    </footer>
  )
}
```

**Step 3: Add portfolio-* CSS classes to app/globals.css**

Append at the end of app/globals.css. Use CSS custom properties for the professional palette.

Professional color palette (Claude's discretion — neutral/dark, no pink):
- Primary text: #0f172a (near-black, Slate 900)
- Secondary text: #475569 (Slate 600)
- Accent: #1e40af (Blue 800 — professional, not playful)
- Background: #ffffff
- Border: #e2e8f0 (Slate 200)
- Nav background: #0f172a (dark nav for professional look)
- Nav text: #f8fafc (near-white)

Font: `-apple-system, BlinkMacSystemFont, 'Segoe UI', 'Helvetica Neue', Arial, sans-serif` (system fonts — professional, fast, NOT Inter)

```css
/* =========================================
   PORTFOLIO SECTION STYLES
   Prefix: portfolio-* (parallel to shop-*)
   NOT for use on shop or comics pages
   ========================================= */

:root {
  --portfolio-color-text: #0f172a;
  --portfolio-color-text-muted: #475569;
  --portfolio-color-accent: #1e40af;
  --portfolio-color-bg: #ffffff;
  --portfolio-color-border: #e2e8f0;
  --portfolio-color-nav-bg: #0f172a;
  --portfolio-color-nav-text: #f8fafc;
  --portfolio-font: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Helvetica Neue', Arial, sans-serif;
}

.portfolio-container {
  max-width: 900px;
  margin: 0 auto;
  padding: 0 1.5rem;
}

/* Navigation */
.portfolio-nav-header {
  background: var(--portfolio-color-nav-bg);
  border-bottom: 1px solid rgba(255,255,255,0.08);
  position: sticky;
  top: 0;
  z-index: 50;
}

.portfolio-nav {
  display: flex;
  justify-content: space-between;
  align-items: center;
  height: 3.5rem;
  font-family: var(--portfolio-font);
}

.portfolio-nav-brand-link {
  font-weight: 700;
  font-size: 0.9rem;
  color: var(--portfolio-color-nav-text);
  text-decoration: none;
  letter-spacing: -0.01em;
}

.portfolio-nav-list {
  display: flex;
  gap: 1.5rem;
  list-style: none;
  margin: 0;
  padding: 0;
}

.portfolio-nav-link {
  font-size: 0.875rem;
  color: rgba(248, 250, 252, 0.75);
  text-decoration: none;
  font-weight: 400;
  transition: color 0.15s;
}

.portfolio-nav-link:hover {
  color: var(--portfolio-color-nav-text);
}

/* Footer */
.portfolio-footer {
  border-top: 1px solid var(--portfolio-color-border);
  padding: 2rem 0;
  margin-top: auto;
  font-family: var(--portfolio-font);
}

.portfolio-footer-inner {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.portfolio-footer-links {
  display: flex;
  gap: 1.25rem;
}

.portfolio-footer-link {
  font-size: 0.875rem;
  color: var(--portfolio-color-text-muted);
  text-decoration: none;
}

.portfolio-footer-link:hover {
  color: var(--portfolio-color-text);
}

.portfolio-footer-copy {
  font-size: 0.8rem;
  color: var(--portfolio-color-text-muted);
  margin: 0;
}

/* Page layout */
.portfolio-page {
  min-height: 100vh;
  font-family: var(--portfolio-font);
  color: var(--portfolio-color-text);
  background: var(--portfolio-color-bg);
}

.portfolio-section {
  padding: 4rem 0;
}

.portfolio-heading-1 {
  font-size: clamp(1.75rem, 4vw, 2.5rem);
  font-weight: 700;
  color: var(--portfolio-color-text);
  margin: 0 0 1rem;
  letter-spacing: -0.02em;
  line-height: 1.15;
}

.portfolio-text-muted {
  color: var(--portfolio-color-text-muted);
  line-height: 1.6;
}
```
  </action>
  <verify>
    `app/components/portfolio-navigation.tsx` exists. `app/components/portfolio-footer.tsx` exists. `grep portfolio- app/globals.css` returns multiple matches. `npx tsc --noEmit` passes without errors.
  </verify>
  <done>Portfolio nav and footer components created. portfolio-* CSS classes exist in globals.css with professional palette (no pink). Shop link present in portfolio footer.</done>
</task>

<task type="auto">
  <name>Task 2: Create (portfolio) route group layout and stub pages with SEO metadata</name>
  <files>
    app/(portfolio)/layout.tsx,
    app/(portfolio)/engineering/page.tsx,
    app/(portfolio)/case-studies/page.tsx,
    app/(portfolio)/resume/page.tsx,
    app/(portfolio)/contact/page.tsx
  </files>
  <action>
**Step 1: Create app/(portfolio)/layout.tsx**

Portfolio layout with base metadata for all portfolio routes. The metadata template produces titles like "Rails Engineer | Engineering" from page-level overrides.

```typescript
import type React from "react"
import type { Metadata } from "next"
import PortfolioNav from "@/app/components/portfolio-navigation"
import PortfolioFooter from "@/app/components/portfolio-footer"

export const metadata: Metadata = {
  title: {
    template: "Rails Engineer | %s",
    default: "Rails Engineer Portfolio",
  },
  description: "Portfolio of a Rails engineer — case studies, tech stack, and resume.",
  authors: [{ name: "ce manalang" }],
  creator: "ce manalang",
  openGraph: {
    type: "website",
    locale: "en_US",
    url: "https://centimentalcomics.com",
    siteName: "centimentalcomics",
    title: "Rails Engineer Portfolio",
    description: "Portfolio of a Rails engineer — case studies, tech stack, and resume.",
    images: [
      {
        url: "/og-portfolio.jpg",
        width: 1200,
        height: 630,
        alt: "Rails Engineer Portfolio",
      },
    ],
  },
  twitter: {
    card: "summary_large_image",
    title: "Rails Engineer Portfolio",
    description: "Portfolio of a Rails engineer — case studies, tech stack, and resume.",
    images: ["/og-portfolio.jpg"],
    creator: "@centimentalcomx",
  },
}

export default function PortfolioLayout({ children }: { children: React.ReactNode }) {
  return (
    <>
      <PortfolioNav />
      <main style={{ flex: 1 }}>
        {children}
      </main>
      <PortfolioFooter />
    </>
  )
}
```

**Step 2: Create stub pages for each portfolio route**

Each page exports its own metadata (overrides the layout template) and renders a minimal placeholder. Content is replaced in Phase 5. Tone is professional — no placeholder humor, just clear functional labels.

**app/(portfolio)/engineering/page.tsx:**
```typescript
import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Engineering",
  description: "Rails, system design, infrastructure, and tools used in production.",
  openGraph: {
    title: "Engineering | Rails Engineer",
    description: "Rails, system design, infrastructure, and tools used in production.",
  },
}

export default function EngineeringPage() {
  return (
    <div className="portfolio-page">
      <section className="portfolio-section">
        <div className="portfolio-container">
          <h1 className="portfolio-heading-1">Engineering</h1>
          <p className="portfolio-text-muted">Tech stack and engineering practices. Content coming in Phase 5.</p>
        </div>
      </section>
    </div>
  )
}
```

**app/(portfolio)/case-studies/page.tsx:**
```typescript
import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Case Studies",
  description: "Production Rails case studies with specific technical outcomes.",
  openGraph: {
    title: "Case Studies | Rails Engineer",
    description: "Production Rails case studies with specific technical outcomes.",
  },
}

export default function CaseStudiesPage() {
  return (
    <div className="portfolio-page">
      <section className="portfolio-section">
        <div className="portfolio-container">
          <h1 className="portfolio-heading-1">Case Studies</h1>
          <p className="portfolio-text-muted">Production case studies. Content coming in Phase 6.</p>
        </div>
      </section>
    </div>
  )
}
```

**app/(portfolio)/resume/page.tsx:**
```typescript
import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Resume",
  description: "Experience, skills, and career progression as a Rails engineer.",
  openGraph: {
    title: "Resume | Rails Engineer",
    description: "Experience, skills, and career progression as a Rails engineer.",
  },
}

export default function ResumePage() {
  return (
    <div className="portfolio-page">
      <section className="portfolio-section">
        <div className="portfolio-container">
          <h1 className="portfolio-heading-1">Resume</h1>
          <p className="portfolio-text-muted">Experience and skills. Content coming in Phase 5.</p>
        </div>
      </section>
    </div>
  )
}
```

**app/(portfolio)/contact/page.tsx:**
```typescript
import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Contact",
  description: "Get in touch about Rails engineering work.",
  openGraph: {
    title: "Contact | Rails Engineer",
    description: "Get in touch about Rails engineering work.",
  },
}

export default function ContactPage() {
  return (
    <div className="portfolio-page">
      <section className="portfolio-section">
        <div className="portfolio-container">
          <h1 className="portfolio-heading-1">Contact</h1>
          <p className="portfolio-text-muted">Contact form coming in Phase 6.</p>
        </div>
      </section>
    </div>
  )
}
```
  </action>
  <verify>
    Run `pnpm dev` and visit each URL:
    - http://localhost:3000/engineering — shows portfolio nav (dark header), portfolio footer with shop link, "Engineering" heading
    - http://localhost:3000/case-studies — shows portfolio nav, "Case Studies" heading
    - http://localhost:3000/resume — shows portfolio nav, "Resume" heading
    - http://localhost:3000/contact — shows portfolio nav, "Contact" heading
    Check page source for /engineering — should contain `<title>Rails Engineer | Engineering</title>` and meta description tag.
    `npx tsc --noEmit` passes.
  </verify>
  <done>All four portfolio routes accessible. Each shows portfolio nav (dark, professional) and footer with shop link. Each page has SEO title and description metadata. No pink color visible. No Inter font used in portfolio section.</done>
</task>

</tasks>

<verification>
After both tasks complete, verify the three-section separation:
1. `/` (comics) — comics nav visible, NO portfolio nav, NO shop nav
2. `/shop` — shop nav visible, NO portfolio nav
3. `/engineering` — portfolio nav visible (dark), NO shop nav, NO comics nav
4. Portfolio footer on all portfolio pages contains "shop" link
5. Page source for /resume contains `<title>Rails Engineer | Resume</title>`
6. No `#ec4899` in portfolio pages (grep globals.css portfolio section to confirm)
</verification>

<success_criteria>
- (portfolio) route group exists with layout, nav, footer
- Four portfolio stub pages accessible at correct URLs
- Portfolio nav shows: home, engineering, case studies, resume, contact
- Portfolio footer shows: shop link (SITE-02 fulfilled)
- SEO metadata present on each portfolio page (SITE-04 fulfilled)
- Professional palette — no pink, system fonts
- TypeScript compiles clean
</success_criteria>

<output>
After completion, create `.planning/phases/04-portfolio-foundation/04-02-SUMMARY.md` with what was done, files changed, and any issues encountered.
</output>
