---
phase: 06-case-studies-contact
plan: "03"
type: execute
wave: 2
depends_on: []
files_modified:
  - app/(portfolio)/contact/actions.ts
  - app/lib/emails/contact-notification.tsx
  - app/lib/rate-limit.ts
  - .env.example
autonomous: true
requirements:
  - CTCT-01
  - CTCT-02

must_haves:
  truths:
    - Contact submission action validates `name`, `email`, and `message` with explicit field-level errors
    - Anti-spam baseline includes honeypot rejection and lightweight rate limiting before email send
    - Successful submission sends notification email through Resend to site-owner contact inbox
    - Delivery failures return retryable error state while preserving entered values
    - Contact flow uses dedicated notification template and dedicated recipient env variable
  artifacts:
    - app/(portfolio)/contact/actions.ts
    - app/lib/emails/contact-notification.tsx
    - app/lib/rate-limit.ts
    - .env.example
  key_links:
    - Server Action contract powers `/contact` UI behavior in Plan 06-04
    - Dedicated contact email template isolates Phase 6 contact concerns from checkout email flow
    - Env contract documents CTCT-02 deployment requirements explicitly
---

<objective>
Build the full contact backend contract for Phase 6: validated submission action, anti-spam controls, and Resend notification delivery with robust failure handling.

Output: Contact backend is integration-ready and fulfills CTCT delivery requirements independently of UI implementation.
</objective>

<execution_context>
@/Users/august/.claude/get-shit-done/workflows/execute-plan.md
@/Users/august/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-case-studies-contact/06-CONTEXT.md
@.planning/phases/06-case-studies-contact/06-RESEARCH.md
@app/(shop)/checkout/actions.ts
@app/lib/emails/order-notification.tsx

Parallelization note:
- This plan is intentionally independent from case-study route work so it can execute in Wave 2 parallel with 06-02.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement contact submission Server Action with validation and anti-spam gates</name>
  <files>
    app/(portfolio)/contact/actions.ts,
    app/lib/rate-limit.ts
  </files>
  <action>
1. Create typed action state contract supporting success, validation error summary, field-level errors, and retryable submission errors.
2. Validate form payload (`name`, `email`, `message`) with Zod and preserve user values on failure.
3. Add honeypot check and lightweight rate-limit guard before invoking Resend.
4. Log structured send failures server-side without exposing internals to UI.
  </action>
  <verify>
    Unit/integration tests or action-level checks confirm validation failures, honeypot rejection, rate-limit rejection, and preserved values on failure.
  </verify>
  <done>CTCT-01 action contract and anti-spam baseline are complete.</done>
</task>

<task type="auto">
  <name>Task 2: Add dedicated Resend notification template and env contract</name>
  <files>
    app/lib/emails/contact-notification.tsx,
    app/(portfolio)/contact/actions.ts,
    .env.example
  </files>
  <action>
1. Create contact-specific notification email template with sender details and message body.
2. Wire Resend send call in action using `RESEND_API_KEY`, `RESEND_FROM_EMAIL`, and dedicated contact recipient variable (for example `CONTACT_NOTIFICATION_EMAIL`).
3. Document required env vars and example values in `.env.example`.
  </action>
  <verify>
    Successful action path triggers Resend send with template payload and returns success state.
  </verify>
  <done>CTCT-02 email notification path is complete and documented.</done>
</task>

</tasks>

<verification>
1. Contact action rejects invalid payload with field errors and summary.
2. Honeypot and rate-limiter prevent spam submissions.
3. Successful submission sends Resend email to configured contact recipient.
4. Send failure returns retryable error while preserving form values.
</verification>

<success_criteria>
- CTCT-01 backend behavior is complete and robust.
- CTCT-02 notification delivery is implemented with explicit configuration contract.
- Plan can merge independently from case-study UX plans.
</success_criteria>

<output>
Create `.planning/phases/06-case-studies-contact/06-03-SUMMARY.md` with action state schema, anti-spam strategy, env requirements, and send success/failure evidence.
</output>
